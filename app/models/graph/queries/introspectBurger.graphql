query introspectBurgerType {
    __type(name: "Menu") {
        name,
        fields {
            name,
            type {
                name,
                ofType { name }
            }
        }
    }
}